# temporary file to set goals to accomplish
#  to be deleted before merging into the main branch

remove print(flush = true)

maybe try making a dft object that encapculates all the parameters of the calculations
so that the object can be directly passed on to function sucha as force and relaxation
that would abstract away the seemingly long list of arguments passed on to these functions
